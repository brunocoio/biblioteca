/**
 * método splice
 */

const nomes = ['ze', 'josney'];
//comeco em indice, quantidade de elementos a serem removidos, elemento a ser add
//nomes.splice(indice, delete, add);
nomes.splice(1, 1, 'roela'); //posicao 1 removi 1 elemento e substitui por string
console.log(nomes);